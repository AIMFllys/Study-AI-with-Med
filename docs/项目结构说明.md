# 🌳 Study AI with Med - 项目架构说明

本项目不仅是一个静态博客，而是基于 **Next.js 16+ (App Router)** 打造的可交互医学 AI 研究图谱。它融合了动态路由、MDX 富文本解析引擎、WASM Python 浏览器运行时，以及统一的语义与 JSON 暴露层。

下面是项目的完整代码结构深度解析：

## 📁 目录一览

```text
d:/project/medicine/
├── public/                          # 🖼️ 静态与全局资源
│   ├── logo.svg                     # 项目 Logo（Navbar + Favicon）
│   └── apple-touch-icon.png         # iOS 主屏图标
│
├── content/                         # 📝 核心命脉：MDX 原创学术库
│   ├── index.mdx                    # 研究区(/research)默认导读页
│   ├── protein-engineering.mdx      # 单体独立研究报告
│   ├── ai-drug-discovery.mdx        # AI 药物发现深度研究
│   ├── medical-llms.mdx             # AI 医疗大模型研究
│   ├── medical-imaging.mdx          # 精准医学影像与数字病理研究
│   ├── agentic-ai-clinical.mdx      # AI 临床智能体研究
│   └── protein-prediction/          # 嵌套目录自动生成层级导航
│       ├── industry-overview.mdx    # 行业全景
│       └── boltz-deployment-guide.mdx  # Boltz 部署实操指南
│
├── src/
│   ├── app/                         # 🌍 Next.js 应用路由出口 (App Router)
│   │   ├── layout.tsx               # 全局 HTML 骨架、主题与字体注入 (ThemeProvider)
│   │   ├── globals.css              # 全局核心样式变量 (Tailwind v4 体系 & 日夜模式)
│   │   ├── page.tsx                 # 酷炫 Landing Page（打字机、书堆装饰、章节卡片）
│   │   ├── about/page.tsx           # 关于页面
│   │   ├── playground/page.tsx      # Python 实验场（占位中）
│   │   ├── research/                # 📚 学术专区路由逻辑
│   │   │   ├── layout.tsx           # 左(树形导航) / 中(正文) / 右(滚动大纲) 的三列布局
│   │   │   ├── page.tsx             # 研究总览页（加载 index.mdx）
│   │   │   └── [...slug]/page.tsx   # 💡 灵魂组件：通配拦截所有 MDX，由 SSR 静态化吐出文章
│   │   └── api/content/route.ts     # 🤖 AI "数字爬虫" 接口：把人类图文转换为大模型所需的 JSON
│   │
│   ├── components/                  # 🧱 可复用组件军火库
│   │   ├── layout/                  # 框架组件
│   │   │   ├── Navbar.tsx           # 顶部导航（桌面+移动底部 Tab Bar，含 LogoIcon SVG）
│   │   │   ├── Sidebar.tsx          # 侧边栏动态路由树（桌面版 + MobileSidebarChips 导航条）
│   │   │   ├── TableOfContents.tsx  # 桌面端大纲（中文编号 + 多层级 + 滚动指示器）
│   │   │   ├── MobileTocFloat.tsx   # 移动端浮动 TOC 按钮 + 侧滑抽屉
│   │   │   ├── Footer.tsx           # 底部页脚
│   │   │   └── ThemeToggle.tsx      # 日/夜模式切换按钮
│   │   └── mdx/                     # 🔮 MDX 专属能力挂载
│   │       ├── index.tsx            # HTML 标签拦截器，所有短代码在这里注册派发
│   │       ├── CodeBlockWrapper.tsx  # 飞书风格代码块（行号/复制/下载/语言切换）
│   │       ├── PythonRunner.tsx     # 浏览器里直接跑代码的「魔法沙盒」 (Pyodide WASM)
│   │       ├── MermaidChart.tsx     # 代码直出学术流转图引擎
│   │       ├── Citation.tsx         # [1] 尾注自动化管理系统
│   │       ├── Callout.tsx          # 强调提示框（Info/Warning/Tip）
│   │       ├── GitHubCard.tsx       # GitHub 仓库卡片展示
│   │       ├── DataTable.tsx        # 自定义表格包装
│   │       └── InteractiveHTML.tsx   # 可交互 HTML/SVG 容器
│   │
│   ├── hooks/                       # 🔧 定制化 React Hooks
│   │   └── useScrollSpy.ts         # 集中化滚动监听（Scroll Spy + 进度条 + body 锁定）
│   │
│   ├── lib/                         # 🧠 业务核心函数与编译器
│   │   ├── mdx.ts                   # Next-MDX-Remote 编译总线 + 共享插件配置 (sharedMdxOptions)
│   │   └── navigation.ts            # 运行时扫描 content 组装目录树的解析器
│   │
│   └── types/                       # 🛡️ TypeScript 类型定义中心
│       └── mdx.d.ts                 # ContentMeta / Reference / HeadingItem / NavItem 类型
│
├── docs/                            # 📖 项目文档
│   ├── 项目结构说明.md               # ← 本文件
│   ├── deploy-guide.md              # 部署指南（GitHub Actions CI/CD）
│   ├── 后端功能规划.md               # 后端 API 规划文档
│   └── 详细AI赋能医学介绍.md         # 项目愿景与学术深度全文
│
├── .github/workflows/               # 🚀 CI/CD 自动部署
├── next.config.ts                   # Turbopack + standalone 产物配置
├── postcss.config.mjs               # PostCSS 解析流（Tailwind v4）
├── eslint.config.mjs                # ESLint 9 扁平配置
├── tsconfig.json                    # TypeScript 严格模式配置
└── package.json                     # 项目运行命脉 (端口 5986)
```

## 🏗️ 架构核心概念

### 数据流

```
content/*.mdx → lib/mdx.ts (gray-matter 解析) → lib/navigation.ts (目录树构建)
                                              ↓
                          research/[...slug]/page.tsx (SSR 渲染)
                                              ↓
                          MDXRemote(sharedMdxOptions) → components/mdx/* (拦截器)
```

### 共享模块设计

| 模块 | 位置 | 用途 |
|------|------|------|
| `sharedMdxOptions` | `lib/mdx.ts` | remark/rehype 插件链的单一真相源 |
| `useScrollSpy` | `hooks/useScrollSpy.ts` | IntersectionObserver + readProgress 统一逻辑 |
| `useScrollToHeading` | `hooks/useScrollSpy.ts` | 平滑跳转 + URL Hash 更新 |
| `useBodyScrollLock` | `hooks/useScrollSpy.ts` | Modal/Drawer 的 body 滚动锁定 |
| `buildNavigationTree` | `lib/navigation.ts` | content/ 目录 → 侧边栏树 |

## 🛠️ 设计哲学摘要

1. **绝对的内容解耦：** 作者只需要在 `content/` 写内容，所有路由、侧边栏和目录表完全依靠 `lib/navigation.ts` 动态扫描编译期搞定，完全零配置。
2. **"真"交互探索：** 脱离图文束缚。读者通过 `<PythonRunner>` 在前沿学术文档里直接用 NumPy；通过 `<MermaidChart>` 读取动态流程状态。
3. **彻底的 AI 原生：** 你不仅可以作为一个人类在这里阅读，这套代码提供了 `/api/content` 作为数字人的大门。任何外部的 LLM AI Agent 都可以结构化调取它需要的文章 JSON 进行精读总结。
4. **DRY 原则：** 所有跨组件复用的逻辑（Scroll Spy、MDX 插件配置）集中到 `hooks/` 和 `lib/` 中的共享模块，杜绝代码重复。

## 📦 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | Next.js 16+ (App Router, Turbopack) |
| UI | React 19 + Tailwind CSS v4 |
| 内容 | MDX + gray-matter + next-mdx-remote |
| 代码高亮 | Shiki (rehype-pretty-code) |
| 公式渲染 | KaTeX (rehype-katex + remark-math) |
| 流程图 | Mermaid.js |
| 代码沙盒 | Pyodide (WebAssembly) |
| 图标 | Lucide React |
| 部署 | GitHub Actions → Aliyun ECS (PM2 + standalone) |
